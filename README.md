# MForge-MUMPS-Vista-IDE

## MForge 0.6.3 — Hakeem Indexing Hotfix

MForge MUMPS & VistA IDE is a modern VS Code IDE toolkit for MUMPS, GT.M/YottaDB, InterSystems-style M code, and VistA/Hakeem development.

The 0.6.3 pre-release hotfix prevents broad `/var/worldvista/prod/hakeem` indexing, repairs Hakeem routine settings to `localr` then `routines`, suppresses missing auto-detect candidate noise, prevents duplicate activation rebuilds, and keeps duplicate routine details debug-only. Do not publish until manual 0.6.3 verification is complete.

See `Src/README.md` for the Marketplace README and feature guide.
